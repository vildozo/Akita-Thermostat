<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <%= f.hidden_field :thermostat_id,:value => @thermostat.id %>
  <div class="field">
    <%= f.label :property, 'Propiedad' %><br>
    <%= f.text_field :property %>
  </div>
  <div class="field">
    <%= f.label :room, 'Habitacion' %><br>
    <%= f.text_field :room %>
  </div>
  <div class="field">
    <%= f.label :city, 'Ciudad' %><br>
    <%= f.select :city, ['Cochabamba', 'La Paz', 'Santa Cruz'] %>

  </div>
  <div class="field">
    <%= f.label :address, 'Direccion' %><br>
    <%= f.text_field :address %>
  </div>
  <div class="actions">
    <%= f.submit "Registrar ubicacion"%>
  </div>
<% end %>
